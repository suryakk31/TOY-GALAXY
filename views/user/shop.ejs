<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shop</title>
  <link rel="stylesheet" href="/css/user/shop.css">
  <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <style>
    
    .bxs-heart {
      color: red;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <div class="filter-navbar">
    <div class="filter-options">
      <h3></h3>
      <ul>
        <li><a href="#" onclick="applyCombinedFilter('popularity')">Popularity</a></li>
        <li><a href="#" onclick="applyCombinedFilter('price-asc')">Price: Low to High</a></li>
        <li><a href="#" onclick="applyCombinedFilter('price-desc')">Price: High to Low</a></li>
        <li><a href="#" onclick="applyCombinedFilter('ratings')">Average Ratings</a></li>
        <li><a href="#" onclick="applyCombinedFilter('featured')">Featured</a></li>
        <li><a href="#" onclick="applyCombinedFilter('new-arrivals')">New Arrivals</a></li>
        <li><a href="#" onclick="applyCombinedFilter('a-z')">A-Z</a></li>
        <li><a href="#" onclick="applyCombinedFilter('z-a')">Z-A</a></li>
      </ul>
    </div>
    <div class="filter-categories">
      <h3></h3>
      <ul>
        <% categories.forEach(category => { %>
          <li><a href="#" onclick="applyCategoryFilter('<%= category._id %>')"><%= category.name %></a></li>
        <% }) %>
      </ul>
    </div>
  </div>
  
    

  <section class="products">
    <% products.forEach(product => { 
      const discountedPrice = product.price - (product.price * product.discount / 100); 
    %>
      <div class="product">
        <div class="product-image-container">
          <a href="/auth/product/<%= product._id %>">
            <% if (product.image && product.image.length > 0) { %>
              <img src="<%= product.image[0] %>" alt="Product Image">
            <% } else { %>
              No Image
            <% } %>
          </a>
          <i class="bx bx-heart heart-icon" data-product-id="<%= product._id %>"></i>

        </div>
        <h3><a href="/product/<%= product._id %>"><%= product.name %></a></h3>
        <p>
          <span style="text-decoration: line-through;">₹<%= product.price %></span> 
          ₹<%= discountedPrice.toFixed(2) %> (<%= product.discount %>% off)
        </p>
        <p><%= product.description %></p>
        <% if (product.stock === 0) { %>
          <p style="color: red;">Not available</p>
      <% } else if (product.stock < 5) { %>
          <p style="color: red;">Only <%= product.stock %> left in stock!</p>
      <% } %>
      
      
      </div>
    <% }); %>
  </section>

 <%- include('../partials/footer') %>


  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>

  <script>
  



    function applyCombinedFilter(filter) {
    const urlParams = new URLSearchParams(window.location.search);
    const categoryId = urlParams.get('category') || '';
    window.location.href = `/shop?filter=${encodeURIComponent(filter)}&category=${encodeURIComponent(categoryId)}`;
  }

  function applyCategoryFilter(categoryId) {
    const urlParams = new URLSearchParams(window.location.search);
    const filter = urlParams.get('filter') || '';
    window.location.href = `/shop?category=${encodeURIComponent(categoryId)}&filter=${encodeURIComponent(filter)}`;
  }
  function searchProducts() {
    var query = document.getElementById('search-input').value;
    window.location.href = `/shop?search=${encodeURIComponent(query)}`;
  }



  // Wait for the DOM to load before attaching event listeners
document.addEventListener('DOMContentLoaded', () => {
  const heartIcons = document.querySelectorAll('.heart-icon');

  heartIcons.forEach(icon => {
    icon.addEventListener('click', async function() {
      const productId = this.getAttribute('data-product-id');

      try {
        // Send an AJAX request to add the product to the wishlist
        const response = await fetch('/wishlist/add', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ productId }),
        });

        const data = await response.json();

        if (data.success) {
          Swal.fire('Success', 'Product added to wishlist!', 'success');
        } else {
          Swal.fire('Error', data.message, 'error');
        }
      } catch (error) {
        console.error('Error adding product to wishlist:', error);
        Swal.fire('Error', 'Something went wrong!', 'error');
      }
    });
  });
});


  
  </script>
</body>
</html>
